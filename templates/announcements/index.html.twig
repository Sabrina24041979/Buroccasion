{% extends 'base.html.twig' %}

{% block title %}Announcements index{% endblock %}

{% block body %}
    <h1>Announcements index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Slug</th>
                <th>Description</th>
                <th>Price</th>
                <th>RelatedProducts</th>
                <th>Brand</th>
                <th>Images</th>
                <th>Status</th>
                <th>Created_at</th>
                <th>Updated_at</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for announcement in announcements %}
            <tr>
                <td>{{ announcement.id }}</td>
                <td>{{ announcement.name }}</td>
                <td>{{ announcement.slug }}</td>
                <td>{{ announcement.description }}</td>
                <td>{{ announcement.price }}</td>
                <td>{{ announcement.relatedProducts }}</td>
                <td>{{ announcement.brand }}</td>
                <td>{{ announcement.images }}</td>
                <td>{{ announcement.status }}</td>
                <td>{{ announcement.createdAt ? announcement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ announcement.updatedAt ? announcement.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_announcements_show', {'id': announcement.id}) }}">show</a>
                    <a href="{{ path('app_announcements_edit', {'id': announcement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_announcements_new') }}">Create new</a>
{% endblock %}
