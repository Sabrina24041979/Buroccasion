{% extends 'base.html.twig' %}

{% block title %}Evaluations{% endblock %}

{% block body %}
    <h1>Evaluations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ evaluation.comment }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ evaluation.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evaluations_index') }}">back to list</a>

    <a href="{{ path('app_evaluations_edit', {'id': evaluation.id}) }}">edit</a>

    {{ include('evaluations/_delete_form.html.twig') }}
{% endblock %}
